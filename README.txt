TCGames.co.in static deployment package

Updated: 18 July 2026

Deploy the contents of this folder at the domain root. The package preserves all existing public routes, uses root-relative internal links, includes a sitemap, robots file, manifest, redirect rules and security headers.

Login, Register and App actions intentionally use the same supplied TC Lottery access URL. The external platform is separate from this independent information website.
