TC GAMES FULL SITE PACKAGE

Primary page:
- /index.html

Supporting pages:
- /tc-game-login/
- /tc-game-register/
- /tc-game-app-download/
- /tc-game-bonus/
- /tc-game-invite-code/
- /tc-game-deposit-withdraw/
- /tc-game-not-working/

Trust and legal pages:
- /about-us/
- /contact-us/
- /privacy-policy/
- /terms-and-conditions/
- /disclaimer/

All supporting pages contain the same Login and Register CTA destination with new-tab and privacy/safety attributes. Legal pages intentionally do not contain external CTA buttons.

The site includes shared assets, favicon files, WebSite/WebPage/Breadcrumb/FAQ structured data where relevant, a complete sitemap, robots.txt, 404 page, manifest and Cloudflare Pages security headers.

Before upload, preserve the directory structure exactly.

URL CLEANUP
- _redirects consolidates slash variants and only the old URLs with a genuinely relevant replacement.
- Unrelated old /f/ articles are intentionally absent, so they return the custom noindex 404 instead of redirecting to the homepage.
- Query-parameter homepage variants are omitted from internal links and sitemap; the homepage self-canonical consolidates them.

FINAL CONTENT AUDIT
- Repeated generic supporting-page paragraphs were rewritten into page-specific copy.
- Shared navigation/footer labels and necessary legal concepts remain consistent by design.
- Titles, descriptions, H1s, canonicals, schema, internal links, CTA attributes and CSP hashes were revalidated.
